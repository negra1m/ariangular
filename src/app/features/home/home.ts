import { Component } from '@angular/core';

/**
 * Home — placeholder da Fase 1.
 *
 * A landing de verdade (hero, 7 cards numerados, atalhos) entra na Fase 4,
 * depois que o conteúdo existir. O que está aqui serve para validar o
 * prerender, o lint de acessibilidade e a paleta nos dois temas.
 */
@Component({
  selector: 'app-home',
  templateUrl: './home.html',
  styleUrl: './home.css',
})
export class Home {}
